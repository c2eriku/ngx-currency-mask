import { NgxCurrencyMaskDirective } from './ngx-currency-mask.directive';

describe('NgxCurrencyMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCurrencyMaskDirective();
    expect(directive).toBeTruthy();
  });
});
